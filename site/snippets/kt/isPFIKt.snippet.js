export default "import web5.sdk.dids.DidResolvers\n// :prepend-end:\n\n/**\n * Tests for Wallet AllowList guide\n */\nclass PfiAllowListTest {\n\n    @Test\n    fun `check if DID has PFI service`() {\n        val pfiDid = TestData.PFI_DID.uri\n\n        // :snippet-start: isPFIKt\n        val isPFI = DidResolvers\n        .resolve(pfiDid)\n        .didDocument\n        ?.service\n        ?.any { it.type == \"PFI\" }\n        ?: false\n        \nval isPFI = DidResolvers\n.resolve(pfiDid)\n.didDocument\n?.service\n?.any { it.type == \"PFI\" }\n?: false\n";